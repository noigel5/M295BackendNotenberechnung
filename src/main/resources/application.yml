server.port: 9090
spring:
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/notenberechnung
    username: postgres
    password: admin
    driverClassName: org.postgresql.Driver